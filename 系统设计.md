以下内容可以直接保存为：

```text
电商报价议价系统设计.md
```

---

# 电商商品报价议价系统设计文档

## 1. 背景与目标

本文档整理关于“电商商品报价、议价系统”的设计讨论，重点围绕以下场景展开：

- 业务类型：电商系统
- 核心对象：用户、商品
- 核心业务：用户直接发起议价，卖家报价，双方协商后形成最终价格
- 一致性要求：通常要强一致
- 架构形态：单体架构
- 技术选型：PHP 与 Java 的取舍
- Java 方案下是否有必要采用 DDD
- 数据库表结构如何设计
- Java DDD 如何落地

---

## 2. 设计前提

### 2.1 业务前提

系统不是简单的“固定价格下单”，而是包含：

- 商品基础价格
- 用户发起议价
- 卖家正式报价
- 多轮协商
- 最终达成一致价格
- 按协商价格下单
- 价格必须可追溯

因此，系统本质上是一个：

> **报价议价 + 交易下单系统**

而不是单纯的商品中心。

---

### 2.2 技术前提

- 单体架构
- 强一致优先
- 初期不建议过度拆分微服务
- 初期不建议过早分库分表
- 优先保证事务边界清晰、状态流转可控、价格来源可追溯

---

## 3. 核心业务概念

## 3.1 议价与报价的区别

### 议价

议价是买家发起的协商请求，表示：

> “我想以某个价格购买这个商品，是否可以？”

它关注的是：

- 谁发起的
- 针对哪个商品
- 数量多少
- 期望价格是多少
- 当前协商到哪一步
- 是否已经谈成

议价是一个**过程性概念**。

---

### 报价

报价是卖家给出的正式价格答复，表示：

> “我可以按这个价格卖给你，报价有效期是多久。”

它关注的是：

- 卖家给出的正式价格
- 报价总金额
- 报价明细
- 报价有效期
- 是否被买家接受
- 是否已经过期或作废

报价是一个**结果性概念**。

---

### 简单例子

商品挂牌价：100 元

1. 买家发起议价：90 元卖吗？
2. 卖家报价：95 元可以卖。
3. 买家继续还价：92 元行吗？
4. 卖家再次报价：最低 93 元。
5. 买家接受：按 93 元成交。

其中：

- 第 1、3 步属于议价过程
- 第 2、4 步属于卖家报价
- 第 5 步形成最终价格协议

---

## 4. 总体架构建议

### 4.1 推荐架构

对于当前场景，推荐采用：

```text
单体应用
├── 用户模块
├── 商品模块
├── 议价模块
├── 报价模块
├── 协议模块
├── 订单模块
└── 库存模块
```

数据库推荐：

```text
MySQL 8.x 单库
```

缓存推荐：

```text
Redis
```

如果后续搜索复杂，可引入：

```text
Elasticsearch
```

如果后续异步能力增强，可引入：

```text
RabbitMQ / RocketMQ / Kafka
```

---

### 4.2 初期不建议做的事

初期不建议：

- 拆分微服务
- 分库分表
- 引入过重的中台架构
- 引入复杂规则引擎
- 一开始就做复杂客户价、阶梯价、合同价体系

初期应优先保证：

- 主流程可跑通
- 价格来源清晰
- 状态流转清晰
- 事务边界清晰
- 库存不会超卖
- 订单价格可追溯

---

## 5. 核心业务域划分

建议将系统划分为以下业务域：

### 5.1 用户域

负责：

- 用户账号
- 用户身份
- 买家/卖家角色
- 用户状态

核心表：

- `users`

---

### 5.2 商品域

负责：

- 商品信息管理
- SPU/SKU 管理
- 商品状态
- 基础价格
- 挂牌价格

核心表：

- `products`
- `product_skus`

---

### 5.3 库存域

负责：

- SKU 可用库存
- 锁定库存
- 库存扣减
- 库存释放
- 库存流水

核心表：

- `inventories`
- `inventory_logs`

---

### 5.4 议价域

负责：

- 买家发起议价
- 记录议价请求
- 记录议价状态
- 记录协商过程

核心表：

- `bargain_requests`
- `bargain_records`

---

### 5.5 报价域

负责：

- 卖家正式报价
- 报价明细
- 报价有效期
- 报价接受/拒绝/过期

核心表：

- `quotations`
- `quotation_items`

---

### 5.6 协议域

负责：

- 保存双方最终达成一致的价格
- 供订单引用
- 管理协议生效、失效、已使用状态

核心表：

- `price_agreements`
- `price_agreement_items`

---

### 5.7 订单域

负责：

- 根据协议创建订单
- 保存订单价格快照
- 管理订单状态

核心表：

- `orders`
- `order_items`
- `order_status_logs`

---

## 6. 核心业务关系

整体关系如下：

```text
用户 users
  |
  | 发起议价
  v
议价申请 bargain_requests
  |
  | 卖家响应
  v
报价单 quotations
  |
  | 报价明细
  v
报价明细 quotation_items
  |
  | 多轮协商
  v
议价记录 bargain_records
  |
  | 达成一致
  v
价格协议 price_agreements
  |
  | 协议明细
  v
价格协议明细 price_agreement_items
  |
  | 下单引用
  v
订单 orders / order_items
```

商品与库存关系如下：

```text
商品 products
  |
  v
SKU product_skus
  |
  v
库存 inventories
```

---

## 7. 数据库表结构设计

以下为核心推荐表结构。

---

## 7.1 核心表清单

| 表名 | 用途 | 是否必须 |
|---|---|---|
| users | 用户表 | 是 |
| products | 商品 SPU 表 | 是 |
| product_skus | 商品 SKU 表 | 是 |
| inventories | 库存表 | 是 |
| bargain_requests | 议价申请表 | 是 |
| quotations | 报价单表 | 是 |
| quotation_items | 报价明细表 | 是 |
| bargain_records | 议价记录表 | 建议必须 |
| price_agreements | 价格协议表 | 是 |
| price_agreement_items | 价格协议明细表 | 是 |
| orders | 订单表 | 是 |
| order_items | 订单明细表 | 是 |
| inventory_logs | 库存流水表 | 强烈建议 |
| order_status_logs | 订单状态日志表 | 强烈建议 |

---

## 7.2 users 用户表

### 用途

存储系统用户，包括买家、卖家、管理员等。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| username | VARCHAR(64) | 登录用户名 |
| phone | VARCHAR(32) | 手机号 |
| email | VARCHAR(128) | 邮箱 |
| password_hash | VARCHAR(255) | 密码哈希 |
| user_type | TINYINT | 用户类型：1买家 2卖家 3管理员 |
| status | TINYINT | 状态：1正常 0禁用 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| version | INT | 乐观锁版本号 |

---

## 7.3 products 商品 SPU 表

### 用途

存储商品主信息，用于商品展示。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| name | VARCHAR(255) | 商品名称 |
| category_id | BIGINT | 分类 ID |
| brand_id | BIGINT | 品牌 ID |
| main_image | VARCHAR(512) | 商品主图 |
| status | TINYINT | 状态：1上架 0下架 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| version | INT | 乐观锁版本号 |

---

## 7.4 product_skus 商品 SKU 表

### 用途

存储商品最小交易单位。  
报价、议价、库存、订单都应围绕 SKU 展开。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| product_id | BIGINT | 所属商品 ID |
| sku_code | VARCHAR(64) | SKU 编码，唯一 |
| sku_name | VARCHAR(255) | SKU 名称 |
| spec_desc | VARCHAR(255) | 规格描述 |
| base_price | DECIMAL(12,2) | 内部参考价/底价 |
| listing_price | DECIMAL(12,2) | 对外挂牌价 |
| min_order_quantity | INT | 最小起订量 |
| currency | VARCHAR(16) | 币种 |
| status | TINYINT | 状态：1在售 0停售 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| version | INT | 乐观锁版本号 |

---

## 7.5 inventories 库存表

### 用途

管理 SKU 的可用库存和锁定库存。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| sku_id | BIGINT | SKU ID，唯一 |
| quantity | INT | 可用库存 |
| locked_quantity | INT | 锁定库存 |
| warning_quantity | INT | 预警库存 |
| version | INT | 乐观锁版本号 |
| updated_at | DATETIME | 更新时间 |

---

## 7.6 bargain_requests 议价申请表

### 用途

存储买家发起的议价请求。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| request_no | VARCHAR(64) | 议价单编号，唯一 |
| buyer_user_id | BIGINT | 买家用户 ID |
| seller_user_id | BIGINT | 卖家用户 ID，可为空 |
| source_type | TINYINT | 来源类型：1商品页 2购物车 3后台代客发起 |
| product_id | BIGINT | 商品 ID |
| sku_id | BIGINT | SKU ID |
| quantity | INT | 议价数量 |
| expected_unit_price | DECIMAL(12,2) | 买家期望单价 |
| expected_total_amount | DECIMAL(12,2) | 买家期望总价 |
| remark | VARCHAR(512) | 备注 |
| status | TINYINT | 状态：0待处理 10已报价 20已关闭 30已达成 |
| expire_at | DATETIME | 过期时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| version | INT | 乐观锁版本号 |

---

## 7.7 quotations 报价单表

### 用途

存储卖家给出的正式报价。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| quotation_no | VARCHAR(64) | 报价单编号，唯一 |
| request_id | BIGINT | 关联议价申请 ID |
| buyer_user_id | BIGINT | 买家用户 ID |
| seller_user_id | BIGINT | 卖家用户 ID |
| source_type | TINYINT | 来源：1议价响应 2卖家主动报价 3询价响应 |
| status | TINYINT | 状态：0草稿 10已报价 20已接受 30已拒绝 40已过期 50已作废 |
| currency | VARCHAR(16) | 币种 |
| total_amount | DECIMAL(12,2) | 报价总金额 |
| discount_amount | DECIMAL(12,2) | 折扣金额 |
| tax_amount | DECIMAL(12,2) | 税额 |
| freight_amount | DECIMAL(12,2) | 运费 |
| valid_until | DATETIME | 报价有效期 |
| reject_reason | VARCHAR(255) | 拒绝原因 |
| remark | VARCHAR(512) | 备注 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| version | INT | 乐观锁版本号 |

---

## 7.8 quotation_items 报价明细表

### 用途

存储报价单中的每个 SKU 的价格和数量。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| quotation_id | BIGINT | 报价单 ID |
| sku_id | BIGINT | SKU ID |
| sku_name | VARCHAR(255) | SKU 名称快照 |
| sku_code | VARCHAR(64) | SKU 编码快照 |
| quantity | INT | 报价数量 |
| unit_price | DECIMAL(12,2) | 报价单价 |
| original_price | DECIMAL(12,2) | 原始价格 |
| discount_amount | DECIMAL(12,2) | 行级折扣金额 |
| tax_rate | DECIMAL(5,2) | 税率 |
| subtotal | DECIMAL(12,2) | 小计金额 |
| remark | VARCHAR(512) | 备注 |
| created_at | DATETIME | 创建时间 |

---

## 7.9 bargain_records 议价记录表

### 用途

记录议价过程中的每一轮动作。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| request_id | BIGINT | 议价申请 ID |
| quotation_id | BIGINT | 报价单 ID |
| initiator_user_id | BIGINT | 动作发起人 ID |
| action_type | TINYINT | 动作类型 |
| old_status | TINYINT | 动作前状态 |
| new_status | TINYINT | 动作后状态 |
| old_total_amount | DECIMAL(12,2) | 原金额 |
| new_total_amount | DECIMAL(12,2) | 新金额 |
| remark | VARCHAR(512) | 备注 |
| created_at | DATETIME | 创建时间 |

### action_type 建议

| 值 | 含义 |
|---|---|
| 1 | 买家发起议价 |
| 2 | 买家还价 |
| 3 | 卖家报价 |
| 4 | 卖家调价 |
| 5 | 买家接受 |
| 6 | 买家拒绝 |
| 7 | 卖家拒绝 |
| 8 | 系统过期 |

---

## 7.10 price_agreements 价格协议表

### 用途

存储双方最终达成一致的价格协议。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| agreement_no | VARCHAR(64) | 协议编号，唯一 |
| quotation_id | BIGINT | 来源报价单 ID |
| buyer_user_id | BIGINT | 买家用户 ID |
| seller_user_id | BIGINT | 卖家用户 ID |
| status | TINYINT | 状态：10生效 20失效 30已使用 |
| total_amount | DECIMAL(12,2) | 协议总金额 |
| currency | VARCHAR(16) | 币种 |
| start_at | DATETIME | 生效时间 |
| end_at | DATETIME | 失效时间 |
| used_at | DATETIME | 使用时间 |
| used_order_id | BIGINT | 使用该协议的订单 ID |
| version | INT | 乐观锁版本号 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

---

## 7.11 price_agreement_items 价格协议明细表

### 用途

存储协议中的每个 SKU 的最终价格。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| agreement_id | BIGINT | 协议 ID |
| quotation_item_id | BIGINT | 来源报价明细 ID |
| sku_id | BIGINT | SKU ID |
| sku_name | VARCHAR(255) | SKU 名称快照 |
| sku_code | VARCHAR(64) | SKU 编码快照 |
| quantity | INT | 数量 |
| unit_price | DECIMAL(12,2) | 协议单价 |
| subtotal | DECIMAL(12,2) | 小计金额 |
| created_at | DATETIME | 创建时间 |

---

## 7.12 orders 订单表

### 用途

存储订单主信息。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| order_no | VARCHAR(64) | 订单编号，唯一 |
| buyer_user_id | BIGINT | 买家用户 ID |
| seller_user_id | BIGINT | 卖家用户 ID |
| agreement_id | BIGINT | 来源协议 ID |
| quotation_id | BIGINT | 来源报价单 ID |
| total_amount | DECIMAL(12,2) | 订单总金额 |
| freight_amount | DECIMAL(12,2) | 运费 |
| discount_amount | DECIMAL(12,2) | 折扣金额 |
| pay_amount | DECIMAL(12,2) | 实付金额 |
| status | TINYINT | 订单状态 |
| receiver_name | VARCHAR(64) | 收货人姓名 |
| receiver_phone | VARCHAR(32) | 收货人电话 |
| receiver_address | VARCHAR(512) | 收货地址 |
| paid_at | DATETIME | 支付时间 |
| delivered_at | DATETIME | 发货时间 |
| completed_at | DATETIME | 完成时间 |
| cancelled_at | DATETIME | 取消时间 |
| cancel_reason | VARCHAR(255) | 取消原因 |
| version | INT | 乐观锁版本号 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

---

## 7.13 order_items 订单明细表

### 用途

存储订单中的商品明细和价格快照。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| order_id | BIGINT | 订单 ID |
| agreement_item_id | BIGINT | 协议明细 ID |
| quotation_item_id | BIGINT | 报价明细 ID |
| sku_id | BIGINT | SKU ID |
| product_name | VARCHAR(255) | 商品名称快照 |
| sku_name | VARCHAR(255) | SKU 名称快照 |
| sku_code | VARCHAR(64) | SKU 编码快照 |
| quantity | INT | 购买数量 |
| unit_price | DECIMAL(12,2) | 成交单价 |
| subtotal | DECIMAL(12,2) | 小计金额 |
| created_at | DATETIME | 创建时间 |

---

## 7.14 inventory_logs 库存流水表

### 用途

记录库存变化，便于追溯。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| sku_id | BIGINT | SKU ID |
| order_id | BIGINT | 关联订单 ID |
| change_type | TINYINT | 变动类型 |
| change_quantity | INT | 变动数量 |
| before_quantity | INT | 变动前可用库存 |
| after_quantity | INT | 变动后可用库存 |
| before_locked | INT | 变动前锁定库存 |
| after_locked | INT | 变动后锁定库存 |
| operator_id | BIGINT | 操作人 ID |
| remark | VARCHAR(512) | 备注 |
| created_at | DATETIME | 创建时间 |

### change_type 建议

| 值 | 含义 |
|---|---|
| 1 | 锁定库存 |
| 2 | 扣减锁定库存 |
| 3 | 释放库存 |
| 4 | 补货 |
| 5 | 人工调整 |

---

## 7.15 order_status_logs 订单状态日志表

### 用途

记录订单状态变化。

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | BIGINT | 主键 |
| order_id | BIGINT | 订单 ID |
| from_status | TINYINT | 原状态 |
| to_status | TINYINT | 新状态 |
| operator_user_id | BIGINT | 操作人 ID |
| remark | VARCHAR(512) | 备注 |
| created_at | DATETIME | 创建时间 |

---

## 8. 状态机设计

## 8.1 议价单状态

```text
0   待处理
10  已报价
20  已关闭
30  已达成
```

流转：

```text
待处理 -> 已报价
已报价 -> 已达成
待处理 -> 已关闭
已报价 -> 已关闭
```

---

## 8.2 报价单状态

```text
0   草稿
10  已报价
20  已接受
30  已拒绝
40  已过期
50  已作废
```

流转：

```text
草稿 -> 已报价
已报价 -> 已接受
已报价 -> 已拒绝
已报价 -> 已过期
已报价 -> 已作废
```

---

## 8.3 价格协议状态

```text
10  生效
20  失效
30  已使用
```

流转：

```text
生效 -> 已使用
生效 -> 失效
```

---

## 8.4 订单状态

```text
0   待支付
10  已支付
20  已发货
30  已完成
40  已取消
```

流转：

```text
待支付 -> 已支付
已支付 -> 已发货
已发货 -> 已完成
待支付 -> 已取消
```

---

## 9. 核心流程设计

## 9.1 用户发起议价

流程：

```text
买家选择商品 SKU
  ↓
填写数量和期望价格
  ↓
提交议价申请
  ↓
写入 bargain_requests
  ↓
写入 bargain_records
```

事务内容：

1. 创建议价单
2. 写入议价记录

---

## 9.2 卖家报价

流程：

```text
卖家查看议价申请
  ↓
填写报价明细
  ↓
生成报价单
  ↓
更新议价单状态为已报价
  ↓
写入议价记录
```

事务内容：

1. 创建报价单
2. 创建报价明细
3. 更新议价单状态
4. 写入议价记录

---

## 9.3 买家接受报价

流程：

```text
买家查看报价单
  ↓
确认接受
  ↓
校验报价状态和有效期
  ↓
报价单状态变为已接受
  ↓
议价单状态变为已达成
  ↓
生成价格协议
  ↓
写入议价记录
```

事务内容：

1. 锁定报价单
2. 更新报价单状态
3. 更新议价单状态
4. 创建价格协议
5. 创建协议明细
6. 写入议价记录

---

## 9.4 根据协议创建订单

流程：

```text
买家选择协议下单
  ↓
校验协议状态
  ↓
协议标记为已使用
  ↓
锁定库存
  ↓
创建订单
  ↓
创建订单明细
  ↓
写入库存流水
```

事务内容：

1. 校验协议有效性
2. 标记协议已使用
3. 锁定库存
4. 创建订单
5. 创建订单明细
6. 写入库存流水

---

## 10. 强一致设计

## 10.1 需要强一致的场景

以下场景必须强一致：

- 用户接受报价
- 生成价格协议
- 创建订单
- 锁定库存
- 支付成功扣减库存
- 取消订单释放库存
- 协议状态变更

---

## 10.2 强一致实现方式

### 单体架构下优先使用本地事务

例如：

```java
@Transactional
public void acceptQuotation() {
    // 更新报价单
    // 更新议价单
    // 创建协议
}
```

---

### 使用乐观锁

适合并发冲突不极端的场景，推荐使用 MyBatis-Plus 的乐观锁插件。

例如：

```sql
UPDATE quotations
SET status = 20, version = version + 1
WHERE id = ?
  AND status = 10
  AND version = ?
```

MyBatis-Plus 配置示例：

```java
@Configuration
public class MybatisPlusConfig {
    
    @Bean
    public OptimisticLockerInterceptor optimisticLockerInterceptor() {
        return new OptimisticLockerInterceptor();
    }
}
```

实体类使用 `@Version` 注解：

```java
@TableName("quotations")
@Data
public class Quotation {
    @TableId
    private Long id;
    
    @Version
    private Integer version;
    
    // 其他字段...
}
```

---

### 库存扣减的乐观锁实现

对于库存扣减场景，可以使用 CAS 方式的更新：

```java
@Mapper
public interface InventoryMapper extends BaseMapper<Inventory> {
    
    /**
     * 扣减库存（乐观锁方式）
     * @param skuId SKU ID
     * @param quantity 扣减数量
     * @return 影响行数
     */
    @Update("UPDATE inventories SET quantity = quantity - #{quantity}, " +
            "version = version + 1 " +
            "WHERE sku_id = #{skuId} AND quantity >= #{quantity}")
    int deductStock(@Param("skuId") Long skuId, @Param("quantity") Integer quantity);
    
    /**
     * 锁定库存（乐观锁方式）
     * @param skuId SKU ID
     * @param quantity 锁定数量
     * @return 影响行数
     */
    @Update("UPDATE inventories SET locked_quantity = locked_quantity + #{quantity}, " +
            "quantity = quantity - #{quantity}, " +
            "version = version + 1 " +
            "WHERE sku_id = #{skuId} AND quantity >= #{quantity}")
    int lockStock(@Param("skuId") Long skuId, @Param("quantity") Integer quantity);
    
    /**
     * 释放库存（乐观锁方式）
     * @param skuId SKU ID
     * @param quantity 释放数量
     * @return 影响行数
     */
    @Update("UPDATE inventories SET locked_quantity = locked_quantity - #{quantity}, " +
            "quantity = quantity + #{quantity}, " +
            "version = version + 1 " +
            "WHERE sku_id = #{skuId} AND locked_quantity >= #{quantity}")
    int releaseStock(@Param("skuId") Long skuId, @Param("quantity") Integer quantity);
}
```

服务层使用示例：

```java
@Service
public class InventoryService {
    
    @Autowired
    private InventoryMapper inventoryMapper;
    
    @Transactional(rollbackFor = Exception.class)
    public void deductStock(Long skuId, Integer quantity) {
        int affected = inventoryMapper.deductStock(skuId, quantity);
        if (affected == 0) {
            throw new BusinessException("库存不足或并发冲突");
        }
        
        // 记录库存流水
        saveInventoryLog(skuId, quantity);
    }
    
    /**
     * 使用 MyBatis-Plus 乐观锁插件的方式
     */
    @Transactional(rollbackFor = Exception.class)
    public void deductStockWithPlugin(Long skuId, Integer quantity) {
        // 查询库存
        Inventory inventory = inventoryMapper.selectById(skuId);
        if (inventory.getQuantity() < quantity) {
            throw new BusinessException("库存不足");
        }
        
        // 更新库存（带版本号检查）
        inventory.setQuantity(inventory.getQuantity() - quantity);
        boolean success = inventoryMapper.updateById(inventory) > 0;
        if (!success) {
            throw new BusinessException("并发冲突，请重试");
        }
        
        // 记录库存流水
        saveInventoryLog(skuId, quantity);
    }
}
```

---

### 使用唯一约束防重

例如：

```sql
UNIQUE KEY uk_quotation_no (quotation_no)
UNIQUE KEY uk_agreement_no (agreement_no)
UNIQUE KEY uk_order_no (order_no)
```

---

## 11. 索引建议

## 11.1 用户表

```sql
UNIQUE KEY uk_username (username)
UNIQUE KEY uk_phone (phone)
```

---

## 11.2 商品 SKU 表

```sql
UNIQUE KEY uk_sku_code (sku_code)
KEY idx_product_id (product_id)
```

---

## 11.3 库存表

```sql
UNIQUE KEY uk_sku_id (sku_id)
```

---

## 11.4 议价申请表

```sql
UNIQUE KEY uk_request_no (request_no)
KEY idx_buyer_status (buyer_user_id, status)
KEY idx_seller_status (seller_user_id, status)
```

---

## 11.5 报价单表

```sql
UNIQUE KEY uk_quotation_no (quotation_no)
KEY idx_request_id (request_id)
KEY idx_buyer_status (buyer_user_id, status)
KEY idx_seller_status (seller_user_id, status)
```

---

## 11.6 报价明细表

```sql
KEY idx_quotation_id (quotation_id)
KEY idx_sku_id (sku_id)
```

---

## 11.7 价格协议表

```sql
UNIQUE KEY uk_agreement_no (agreement_no)
KEY idx_quotation_id (quotation_id)
KEY idx_buyer_status (buyer_user_id, status)
```

---

## 11.8 订单表

```sql
UNIQUE KEY uk_order_no (order_no)
KEY idx_buyer_status (buyer_user_id, status)
KEY idx_agreement_id (agreement_id)
KEY idx_quotation_id (quotation_id)
```

---

## 12. PHP 与 Java 选型

## 12.1 结论

如果团队更熟悉 PHP，并且希望快速上线：

```text
PHP + Laravel + MySQL + Redis
```

完全可行。

如果系统会长期迭代，并且业务复杂度会持续增加：

```text
Java + Spring Boot + MySQL + Redis
```

更推荐。

---

## 12.2 PHP 适合场景

- 小团队
- 预算有限
- 快速上线
- 团队熟悉 PHP
- 单体系统为主
- 业务复杂度中等

推荐技术栈：

```text
PHP 8.x
Laravel
MySQL 8
Redis
Nginx
Laravel Queue
```

---

## 12.3 Java 适合场景

- 长期核心业务系统
- 业务规则复杂
- 状态流转复杂
- 多人协作
- 后期可能接财务、合同、审批、对账
- 对可维护性要求高

推荐技术栈：

```text
Java 17 / 21
Spring Boot 3.x
MySQL 8
Redis
MyBatis-Plus (替代 MyBatis/JPA)
Flyway
Lombok
MapStruct
```

---

## 12.4 最终建议

如果团队两个都能做，建议：

```text
优先 Java
```

如果团队主要熟悉 PHP，并且需要快速交付：

```text
选择 PHP
```

强一致的关键不在语言，而在于：

- 事务设计
- 并发控制
- 幂等设计
- 状态机设计
- 日志流水
- 价格快照

---

## 13. Java 是否有必要使用 DDD

## 13.1 结论

有必要使用 DDD 的思想，但不建议一开始就上重型 DDD。

推荐：

```text
轻量 DDD
```

即：

- 清晰业务域划分
- 清晰聚合边界
- 领域对象包含业务行为
- 应用服务负责编排
- 基础设施负责持久化
- 不盲目引入复杂 DDD 全家桶

---

## 13.2 为什么适合 DDD 思想

因为该业务不是简单 CRUD，而是包含：

- 议价
- 报价
- 多轮协商
- 状态流转
- 协议达成
- 订单生成
- 库存锁定
- 强一致要求

如果全部使用传统贫血模型和超大 Service，后期容易出现：

- 业务规则散落各处
- 状态流转不可控
- 代码难以维护
- 改一处影响多处
- 无法追溯业务动作

---

## 14. Java DDD 落地方案

## 14.1 推荐架构

```text
interfaces        接口层
application       应用层
domain            领域层
infrastructure    基础设施层
```

---

## 14.2 包结构建议

```text
com.xxx.bargain
├── interfaces
│   ├── rest
│   └── dto
│
├── application
│   ├── bargain
│   ├── quotation
│   ├── agreement
│   └── order
│
├── domain
│   ├── shared
│   ├── bargain
│   ├── quotation
│   ├── agreement
│   ├── order
│   └── inventory
│
└── infrastructure
    ├── persistence
    ├── cache
    ├── event
    └── config
```

---

## 15. 聚合划分

## 15.1 BargainRequest 议价聚合

### 职责

表示一次议价请求。

### 核心字段

- requestNo
- buyerUserId
- sellerUserId
- skuId
- quantity
- expectedUnitPrice
- status

### 核心行为

- markQuoted
- markAgreed
- close

---

## 15.2 Quotation 报价聚合

### 职责

表示一次正式报价。

### 聚合内实体

- QuotationItem

### 核心字段

- quotationNo
- bargainRequestId
- buyerUserId
- sellerUserId
- status
- totalAmount
- validUntil

### 核心行为

- submit
- accept
- reject
- expire
- cancel

---

## 15.3 PriceAgreement 协议聚合

### 职责

表示双方最终达成一致的价格。

### 聚合内实体

- PriceAgreementItem

### 核心行为

- createFrom
- markUsed
- invalidate

---

## 15.4 Order 订单聚合

### 职责

表示最终交易订单。

### 聚合内实体

- OrderItem

### 核心行为

- createFrom
- pay
- cancel
- ship
- complete

---

## 15.5 Inventory 库存聚合

### 职责

表示 SKU 库存。

### 核心行为

- lock
- deductLocked
- release
- replenish

---

## 16. 领域对象示例

## 16.1 报价聚合根

```java
public class Quotation {

    private Long id;
    private String quotationNo;
    private Long bargainRequestId;
    private Long buyerUserId;
    private Long sellerUserId;
    private QuotationStatus status;
    private BigDecimal totalAmount;
    private LocalDateTime validUntil;
    private Integer version;
    private List<QuotationItem> items = new ArrayList<>();

    public void submit() {
        if (this.status != QuotationStatus.DRAFT) {
            throw new BusinessException("只有草稿状态可以提交");
        }
        this.status = QuotationStatus.SUBMITTED;
    }

    public void accept() {
        if (this.status != QuotationStatus.SUBMITTED) {
            throw new BusinessException("当前报价单不可接受");
        }
        if (validUntil != null && validUntil.isBefore(LocalDateTime.now())) {
            throw new BusinessException("报价已过期");
        }
        this.status = QuotationStatus.ACCEPTED;
    }

    public void reject() {
        if (this.status != QuotationStatus.SUBMITTED) {
            throw new BusinessException("当前报价单不可拒绝");
        }
        this.status = QuotationStatus.REJECTED;
    }

    public void expire() {
        if (this.status != QuotationStatus.SUBMITTED) {
            return;
        }
        this.status = QuotationStatus.EXPIRED;
    }
}
```

---

## 16.2 协议聚合根

```java
public class PriceAgreement {

    private Long id;
    private String agreementNo;
    private Long quotationId;
    private Long buyerUserId;
    private Long sellerUserId;
    private AgreementStatus status;
    private BigDecimal totalAmount;
    private LocalDateTime startAt;
    private LocalDateTime endAt;
    private Integer version;
    private List<PriceAgreementItem> items = new ArrayList<>();

    public static PriceAgreement createFrom(Quotation quotation) {
        PriceAgreement agreement = new PriceAgreement();
        agreement.agreementNo = generateAgreementNo();
        agreement.quotationId = quotation.getId();
        agreement.buyerUserId = quotation.getBuyerUserId();
        agreement.sellerUserId = quotation.getSellerUserId();
        agreement.status = AgreementStatus.EFFECTIVE;
        agreement.totalAmount = quotation.getTotalAmount();
        agreement.startAt = LocalDateTime.now();
        agreement.endAt = LocalDateTime.now().plusDays(7);

        for (QuotationItem item : quotation.getItems()) {
            agreement.items.add(PriceAgreementItem.createFrom(item));
        }

        return agreement;
    }

    public void markUsed() {
        if (this.status != AgreementStatus.EFFECTIVE) {
            throw new BusinessException("协议不可用");
        }
        this.status = AgreementStatus.USED;
    }

    private static String generateAgreementNo() {
        return "PA" + System.currentTimeMillis();
    }
}
```

---

## 16.3 订单聚合根

```java
public class Order {

    private Long id;
    private String orderNo;
    private Long buyerUserId;
    private Long sellerUserId;
    private Long agreementId;
    private BigDecimal totalAmount;
    private OrderStatus status;
    private Integer version;
    private List<OrderItem> items = new ArrayList<>();

    public static Order createFrom(PriceAgreement agreement) {
        Order order = new Order();
        order.orderNo = generateOrderNo();
        order.buyerUserId = agreement.getBuyerUserId();
        order.sellerUserId = agreement.getSellerUserId();
        order.agreementId = agreement.getId();
        order.totalAmount = agreement.getTotalAmount();
        order.status = OrderStatus.PENDING_PAYMENT;

        for (PriceAgreementItem item : agreement.getItems()) {
            order.items.add(OrderItem.createFrom(item));
        }

        return order;
    }

    public void pay() {
        if (this.status != OrderStatus.PENDING_PAYMENT) {
            throw new BusinessException("订单状态不允许支付");
        }
        this.status = OrderStatus.PAID;
    }

    public void cancel() {
        if (this.status != OrderStatus.PENDING_PAYMENT) {
            throw new BusinessException("订单状态不允许取消");
        }
        this.status = OrderStatus.CANCELED;
    }

    private static String generateOrderNo() {
        return "OD" + System.currentTimeMillis();
    }
}
```

---

## 16.4 库存聚合根

```java
public class Inventory {

    private Long id;
    private Long skuId;
    private Integer quantity;
    private Integer lockedQuantity;
    private Integer version;

    public void lock(int quantity) {
        if (this.quantity < quantity) {
            throw new BusinessException("库存不足");
        }
        this.quantity -= quantity;
        this.lockedQuantity += quantity;
    }

    public void deductLocked(int quantity) {
        if (this.lockedQuantity < quantity) {
            throw new BusinessException("锁定库存不足");
        }
        this.lockedQuantity -= quantity;
    }

    public void release(int quantity) {
        if (this.lockedQuantity < quantity) {
            throw new BusinessException("锁定库存不足");
        }
        this.lockedQuantity -= quantity;
        this.quantity += quantity;
    }
}
```

---

## 17. 应用服务示例

## 17.1 接受报价

```java
@Service
public class QuotationApplicationService {

    private final QuotationRepository quotationRepository;
    private final BargainRequestRepository bargainRequestRepository;
    private final PriceAgreementRepository agreementRepository;
    private final BargainRecordRepository bargainRecordRepository;

    public QuotationApplicationService(
        QuotationRepository quotationRepository,
        BargainRequestRepository bargainRequestRepository,
        PriceAgreementRepository agreementRepository,
        BargainRecordRepository bargainRecordRepository
    ) {
        this.quotationRepository = quotationRepository;
        this.bargainRequestRepository = bargainRequestRepository;
        this.agreementRepository = agreementRepository;
        this.bargainRecordRepository = bargainRecordRepository;
    }

    @Transactional
    public Long acceptQuotation(AcceptQuotationCommand command) {
        Quotation quotation = quotationRepository.findByIdForUpdate(
            command.getQuotationId()
        );

        quotation.accept();

        BargainRequest request = bargainRequestRepository.findByIdForUpdate(
            quotation.getBargainRequestId()
        );

        request.markAgreed();

        PriceAgreement agreement = PriceAgreement.createFrom(quotation);

        quotationRepository.save(quotation);
        bargainRequestRepository.save(request);
        agreementRepository.save(agreement);

        bargainRecordRepository.save(
            BargainRecord.buyerAccepted(request.getId(), quotation.getId())
        );

        return agreement.getId();
    }
}
```

---

## 17.2 根据协议创建订单

```java
@Service
public class OrderApplicationService {

    private final PriceAgreementRepository agreementRepository;
    private final OrderRepository orderRepository;
    private final InventoryRepository inventoryRepository;

    public OrderApplicationService(
        PriceAgreementRepository agreementRepository,
        OrderRepository orderRepository,
        InventoryRepository inventoryRepository
    ) {
        this.agreementRepository = agreementRepository;
        this.orderRepository = orderRepository;
        this.inventoryRepository = inventoryRepository;
    }

    @Transactional
    public Long createOrder(CreateOrderCommand command) {
        PriceAgreement agreement = agreementRepository.findById(
            command.getAgreementId()
        );

        agreement.markUsed();

        for (PriceAgreementItem item : agreement.getItems()) {
            Inventory inventory = inventoryRepository.findBySkuIdForUpdate(
                item.getSkuId()
            );
            inventory.lock(item.getQuantity());
            inventoryRepository.save(inventory);
        }

        Order order = Order.createFrom(agreement);
        orderRepository.save(order);

        return order.getId();
    }
}
```

---

## 18. 事务边界建议

## 18.1 可放在同一事务中的操作

### 接受报价

```text
更新报价单状态
更新议价单状态
创建价格协议
创建协议明细
写入议价记录
```

---

### 创建订单

```text
标记协议已使用
锁定库存
创建订单
创建订单明细
写入库存流水
```

---

## 18.2 不建议放在核心事务中的操作

以下操作建议异步处理：

- 发短信
- 发站内信
- 加积分
- 同步搜索索引
- 数据统计
- 推送通知
- 日志归档

---

## 19. 实施路线建议

## 第一阶段：完成核心主流程

目标：

```text
用户发起议价
卖家报价
买家接受报价
生成协议
根据协议下单
```

涉及表：

```text
users
products
product_skus
bargain_requests
quotations
quotation_items
price_agreements
price_agreement_items
orders
order_items
```

---

## 第二阶段：补齐强一致能力

目标：

- 防止并发重复接受
- 防止库存超卖
- 保证状态流转可控
- 保证价格可追溯

涉及能力：

- 乐观锁（MyBatis-Plus @Version）
- 唯一约束
- 状态日志
- 库存流水
- 订单状态日志
- 报价有效期校验
- 幂等控制

---

## 第三阶段：补齐扩展能力

目标：

- 支持更复杂议价
- 支持后台管理
- 支持订单履约
- 支持运营统计

涉及能力：

- 多轮议价记录
- 报价过期任务
- 订单取消释放库存
- 支付成功扣减锁定库存
- 退款流程
- 后台查询
- 数据报表

---

## 20. 最终建议

### 数据库设计

建议采用：

```text
MySQL 单库
```

核心表包括：

```text
users
products
product_skus
inventories
bargain_requests
quotations
quotation_items
bargain_records
price_agreements
price_agreement_items
orders
order_items
inventory_logs
order_status_logs
```

---

### 技术选型

如果团队熟悉 PHP：

```text
PHP + Laravel
```

如果系统长期演进：

```text
Java + Spring Boot
```

---

### Java 架构方案

推荐：

```text
轻量 DDD
```

核心要点：

- 按业务域分包
- 聚合根管理状态
- 应用服务编排流程
- 领域对象承载业务规则
- 事务边界清晰
- 状态机统一控制
- 价格和库存变更必须留痕

---

## 21. 核心设计原则总结

1. 商品价格、报价价格、协议价格、订单价格必须分开。
2. 订单价格必须是快照，不能后续被商品改价影响。
3. 议价是过程，报价是结果。
4. 报价单必须有状态和有效期。
5. 协议必须是可下单的正式价格依据。
6. 订单必须记录来源报价和来源协议。
7. 库存变更必须有流水。
8. 订单状态变更必须有日志。
9. 强一致优先使用本地事务。
10. 初期不要过度设计，优先保证主流程正确。