<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.quotation.domain.repository.QuotationRepository">

    <resultMap id="QuotationResultMap" type="com.example.quotation.domain.model.Quotation">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="original_price" property="originalPrice"/>
        <result column="current_price" property="currentPrice"/>
        <result column="min_acceptable_price" property="minAcceptablePrice"/>
        <result column="stock_quantity" property="stockQuantity"/>
        <result column="status" property="status"/>
        <result column="version" property="version"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectById" resultMap="QuotationResultMap">
        SELECT * FROM quotation WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quotation (
            product_id,
            product_name,
            original_price,
            current_price,
            min_acceptable_price,
            stock_quantity,
            status,
            version,
            created_at,
            updated_at,
            deleted
        ) VALUES (
            #{productId},
            #{productName},
            #{originalPrice},
            #{currentPrice},
            #{minAcceptablePrice},
            #{stockQuantity},
            #{status},
            #{version},
            NOW(),
            NOW(),
            0
        )
    </insert>

</mapper>
